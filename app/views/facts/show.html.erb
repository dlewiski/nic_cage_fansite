<div class="container">
  <h2><%= @fact.title %></h2>
  <h3>Rating: <%= @fact.rating %></h3>
</div>


<p><%= link_to "Delete fact", fact_path(@fact),
                        :data => {:confirm => "Are you sure?",
                                       :method => "delete"} %></p>
